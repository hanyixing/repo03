# Loaded by `vue-cli-service build` (production mode).
# Empty base => same-origin; in production the frontend is served by Spring Boot,
# so API requests hit the same host that served the page.
VUE_APP_BASE_API=
