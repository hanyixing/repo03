# Copy this file to .env (which is gitignored) and set real values.
# Consumed by docker-compose.yml and deploy.sh.

# MySQL root password. It is also passed to the backend (DB_PASSWORD) so the
# Spring Boot prod profile can connect. Use a strong value in real deployments.
DB_PASSWORD=change-me-please
