# Development profile - local MySQL, convenience credentials (non-production).
spring:
  datasource:
    url: 'jdbc:mysql://localhost:3306/vueblog2?useUnicode=true&characterEncoding=UTF-8&serverTimezone=Asia/Shanghai'
    username: root
    password: 123
logging:
  level:
    org.sang: debug
